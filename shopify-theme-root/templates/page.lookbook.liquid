<!-- Lookbook Hub Page - Links to Studio & Street Lookbooks -->
<div class="lookbook-hub" id="lookbookHub">
  <!-- Hero Section -->
  <section class="lookbook-hub__hero">
    <div class="lookbook-hub__hero-content">
      <p class="lookbook-hub__tagline">Season 01</p>
      <h1 class="lookbook-hub__title">LOOKBOOK</h1>
      <p class="lookbook-hub__subtitle">Two vibes. One vision. Choose your path.</p>
    </div>
  </section>

  <!-- Split Choice Section -->
  <section class="lookbook-hub__choices">
    <!-- Studio Side -->
    <a href="/pages/studio-lookbook" class="lookbook-choice lookbook-choice--studio">
      <div class="lookbook-choice__background">
        <img src="{{ 'studio_v2_1_night_out_flash_1765015704274.png' | asset_url }}" alt="Studio Lookbook">
        <div class="lookbook-choice__overlay lookbook-choice__overlay--studio"></div>
      </div>
      <div class="lookbook-choice__content">
        <span class="lookbook-choice__label">Collection 01</span>
        <h2 class="lookbook-choice__title">STUDIO</h2>
        <p class="lookbook-choice__desc">Essential fits. Elevated basics. Soft silhouettes for the modern woman.</p>
        <span class="lookbook-choice__cta">View Lookbook →</span>
      </div>
    </a>

    <!-- Divider -->
    <div class="lookbook-hub__divider">
      <span>OR</span>
    </div>

    <!-- Street Side -->
    <a href="/pages/street-lookbook" class="lookbook-choice lookbook-choice--street">
      <div class="lookbook-choice__background">
        <img src="{{ 'vibe-urban.png' | asset_url }}" alt="Street Lookbook">
        <div class="lookbook-choice__overlay lookbook-choice__overlay--street"></div>
      </div>
      <div class="lookbook-choice__content">
        <span class="lookbook-choice__label">Division 001</span>
        <h2 class="lookbook-choice__title">STREET</h2>
        <p class="lookbook-choice__desc">Heavyweight cottons. Industrial cuts. Built for the concrete.</p>
        <span class="lookbook-choice__cta">View Lookbook →</span>
      </div>
    </a>
  </section>

  <!-- Preview Grid -->
  <section class="lookbook-hub__preview">
    <h3 class="lookbook-hub__preview-title">Latest Looks</h3>
    <div class="lookbook-hub__grid">
      <div class="lookbook-preview">
        <img src="{{ 'studio_v2_2_park_cozy_1765015718679.png' | asset_url }}" alt="Golden Hour">
        <span class="lookbook-preview__tag">Studio</span>
      </div>
      <div class="lookbook-preview">
        <img src="{{ 'trip-chrome.png' | asset_url }}" alt="Chrome Dreams">
        <span class="lookbook-preview__tag lookbook-preview__tag--street">Street</span>
      </div>
      <div class="lookbook-preview">
        <img src="{{ 'studio-lookbook-placeholder.png' | asset_url }}" alt="Coming Soon">
        <span class="lookbook-preview__tag">Studio</span>
      </div>
      <div class="lookbook-preview">
        <img src="{{ 'street-lookbook-placeholder.png' | asset_url }}" alt="Coming Soon">
        <span class="lookbook-preview__tag lookbook-preview__tag--street">Street</span>
      </div>
    </div>
  </section>

  <!-- Back Link -->
  <nav class="lookbook-hub__nav">
    <a href="/" class="lookbook-hub__back">← Back to Home</a>
  </nav>
</div>

<style>
  .lookbook-hub {
    min-height: 100vh;
    background: var(--background);
    color: var(--foreground);
  }

  /* Hero */
  .lookbook-hub__hero {
    padding: 8rem 2rem 4rem;
    text-align: center;
  }

  .lookbook-hub__tagline {
    font-family: var(--font-display);
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.3em;
    opacity: 0.5;
    margin-bottom: 1rem;
  }

  .lookbook-hub__title {
    font-family: var(--font-display);
    font-size: clamp(3rem, 10vw, 8rem);
    font-weight: 700;
    letter-spacing: -0.04em;
    margin-bottom: 1rem;
  }

  .lookbook-hub__subtitle {
    font-size: 1.25rem;
    opacity: 0.7;
  }

  /* Split Choices */
  .lookbook-hub__choices {
    display: flex;
    min-height: 60vh;
    position: relative;
  }

  .lookbook-choice {
    flex: 1;
    position: relative;
    overflow: hidden;
    text-decoration: none;
    color: #fff;
    display: flex;
    align-items: flex-end;
    padding: 3rem;
    min-height: 500px;
  }

  .lookbook-choice__background {
    position: absolute;
    inset: 0;
  }

  .lookbook-choice__background img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .lookbook-choice:hover .lookbook-choice__background img {
    transform: scale(1.05);
  }

  .lookbook-choice__overlay--studio {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(250,240,235,0.95) 0%, rgba(250,240,235,0.5) 50%, rgba(250,240,235,0.2) 100%);
  }

  .lookbook-choice__overlay--street {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(10,10,10,0.95) 0%, rgba(10,10,10,0.5) 50%, rgba(10,10,10,0.2) 100%);
  }

  .lookbook-choice__content {
    position: relative;
    z-index: 10;
    max-width: 400px;
  }

  .lookbook-choice--studio .lookbook-choice__content {
    color: #1A1A1A;
  }

  .lookbook-choice__label {
    display: block;
    font-family: var(--font-display);
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    margin-bottom: 0.5rem;
  }

  .lookbook-choice--studio .lookbook-choice__label {
    color: #E91E63;
  }

  .lookbook-choice--street .lookbook-choice__label {
    color: #4ade80;
  }

  .lookbook-choice__title {
    font-family: var(--font-display);
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 700;
    letter-spacing: -0.02em;
    margin-bottom: 1rem;
  }

  .lookbook-choice__desc {
    font-size: 1rem;
    line-height: 1.6;
    margin-bottom: 1.5rem;
    opacity: 0.8;
  }

  .lookbook-choice__cta {
    font-family: var(--font-display);
    font-size: 0.875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    padding-bottom: 0.25rem;
    transition: all 0.3s ease;
  }

  .lookbook-choice--studio .lookbook-choice__cta {
    border-bottom: 2px solid #E91E63;
  }

  .lookbook-choice--street .lookbook-choice__cta {
    border-bottom: 2px solid #4ade80;
  }

  .lookbook-choice:hover .lookbook-choice__cta {
    padding-left: 0.5rem;
  }

  /* Divider */
  .lookbook-hub__divider {
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    z-index: 20;
    width: 60px;
    height: 60px;
    background: var(--background);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-display);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    color: var(--foreground);
  }

  /* Preview Grid */
  .lookbook-hub__preview {
    padding: 6rem 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .lookbook-hub__preview-title {
    font-family: var(--font-display);
    font-size: 0.875rem;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    text-align: center;
    margin-bottom: 3rem;
    opacity: 0.5;
  }

  .lookbook-hub__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
  }

  .lookbook-preview {
    position: relative;
    aspect-ratio: 3/4;
    overflow: hidden;
    background: rgba(0,0,0,0.1);
  }

  .lookbook-preview img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s ease;
  }

  .lookbook-preview:hover img {
    transform: scale(1.05);
  }

  .lookbook-preview__tag {
    position: absolute;
    bottom: 1rem;
    left: 1rem;
    font-family: var(--font-display);
    font-size: 0.625rem;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    background: #E91E63;
    color: #fff;
    padding: 0.375rem 0.75rem;
    border-radius: 2px;
  }

  .lookbook-preview__tag--street {
    background: #4ade80;
    color: #0A0A0A;
  }

  /* Navigation */
  .lookbook-hub__nav {
    padding: 2rem;
    text-align: center;
    border-top: 1px solid rgba(128,128,128,0.2);
  }

  .lookbook-hub__back {
    font-family: var(--font-display);
    font-size: 0.875rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--foreground);
    text-decoration: none;
    opacity: 0.6;
    transition: opacity 0.3s ease;
  }

  .lookbook-hub__back:hover {
    opacity: 1;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .lookbook-hub__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .lookbook-hub__choices {
      flex-direction: column;
    }

    .lookbook-choice {
      min-height: 400px;
    }

    .lookbook-hub__divider {
      left: 50%;
      top: auto;
      bottom: calc(50% - 30px);
    }

    .lookbook-hub__grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 1rem;
    }
  }
</style>

<script>
  // Auto-redirect based on theme (optional - commented out by default)
  // Uncomment if you want automatic redirection
  /*
  document.addEventListener('DOMContentLoaded', () => {
    const theme = document.documentElement.dataset.theme;
    if (theme === 'dark') {
      window.location.href = '/pages/street-lookbook';
    } else {
      window.location.href = '/pages/studio-lookbook';
    }
  });
  */
</script>
