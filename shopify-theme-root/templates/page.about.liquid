<!-- About Page Template -->
<div class="page-section about-page">
  <div class="page-content">
    <header class="page-header">
      <p class="page-header__tagline">Our Story</p>
      <h1 class="page-header__title">ABOUT</h1>
    </header>
    
    <div class="about-content">
      <div class="about-intro">
        <p class="about-intro__lead">
          RETITLED was born from a simple idea: your wardrobe should tell your story, not someone else's.
        </p>
        <p class="about-intro__text">
          We create essential pieces that become the foundation of your personal style. No logos screaming for attention. No trends dictating your choices. Just quality construction, thoughtful design, and pieces that feel like they were made for you.
        </p>
      </div>
      
      <div class="about-values">
        <div class="about-value">
          <span class="about-value__number">01</span>
          <h3 class="about-value__title">Quality First</h3>
          <p class="about-value__text">14oz heavyweight cotton. LA Apparel blanks. Built to last seasons, not weeks.</p>
        </div>
        <div class="about-value">
          <span class="about-value__number">02</span>
          <h3 class="about-value__title">No Waste</h3>
          <p class="about-value__text">Limited drops. Made to order when possible. We don't believe in excess inventory.</p>
        </div>
        <div class="about-value">
          <span class="about-value__number">03</span>
          <h3 class="about-value__title">Two Vibes</h3>
          <p class="about-value__text">Street for the concrete. Studio for the lifestyle. One brand, two expressions.</p>
        </div>
      </div>
      
      <div class="about-cta">
        <a href="/" class="btn btn-primary">Explore Collections</a>
      </div>
    </div>
  </div>
</div>

<style>
  .about-page {
    min-height: 100vh;
    padding: 6rem 2rem;
    background: var(--background);
  }
  
  .about-content {
    max-width: 800px;
    margin: 0 auto;
  }
  
  .about-intro {
    margin-bottom: 4rem;
    text-align: center;
  }
  
  .about-intro__lead {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 4vw, 2.5rem);
    line-height: 1.3;
    color: var(--foreground);
    margin-bottom: 2rem;
  }
  
  .about-intro__text {
    font-size: 1.125rem;
    color: var(--foreground);
    opacity: 0.7;
    line-height: 1.8;
    max-width: 600px;
    margin: 0 auto;
  }
  
  .about-values {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 3rem;
    margin-bottom: 4rem;
  }
  
  .about-value {
    text-align: center;
  }
  
  .about-value__number {
    font-family: var(--font-display);
    font-size: 0.75rem;
    color: var(--accent);
    letter-spacing: 0.2em;
    display: block;
    margin-bottom: 1rem;
  }
  
  .about-value__title {
    font-family: var(--font-display);
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--foreground);
    margin-bottom: 0.75rem;
  }
  
  .about-value__text {
    font-size: 0.9375rem;
    color: var(--foreground);
    opacity: 0.6;
    line-height: 1.6;
  }
  
  .about-cta {
    text-align: center;
  }
  
  @media (max-width: 768px) {
    .about-values {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
  }
</style>
